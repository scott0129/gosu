interface BeatmapMetadata {
    beatmap_id: number,
    artist: string,
    title: string,
    preview_url: string,
    set_id: number,
    creator: string,
    version: string,
}

export default BeatmapMetadata
