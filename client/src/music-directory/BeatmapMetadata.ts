interface BeatmapMetadata {
    artist: string,
    beatmap_id: number,
    preview_url: string,
    creator: string,
    set_id: number,
    title: string,
    version: string,
}

export default BeatmapMetadata
